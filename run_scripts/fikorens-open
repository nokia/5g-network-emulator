#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ $# -lt 1 ]]; then
    echo "Usage: $(basename "$0") <ueN|dn|emu>" >&2
    exit 1
fi

sudo -E "${ROOT_DIR}/run_scripts/run_fikore_nfqueue_lab.sh" shell "$1"
